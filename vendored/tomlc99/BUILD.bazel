load("//:BUILD.bzl",
     "SRCS", "DEPS", "COPTS", "INCLUDE_PATHS", "DEFINES", "LINKOPTS")

cc_library(
    name       = "tomlc99",
    linkstatic = True,
    srcs       = SRCS + ["toml.c", "tomlx.c"],
    hdrs       = ["toml.h", "tomlx.h"],
    deps       = DEPS,
    copts      = COPTS + INCLUDE_PATHS,
    linkopts   = LINKOPTS,
    visibility = ["//visibility:public"]
)
