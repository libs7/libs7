; mibl config

[mibl]
version = 0.1.0
grammar=1.0.0-BETA  ;; version number of config grammar, not obazl program

emit = none

debug = foo,bar

[repl]
;; TODO: repl flag to skip parsetree crawl?
;; TODO: repl flag to skip parsetree->mibl step

[scripts]

;;dir = /foo/bar

[srcs]
;;  TODO: rename ocaml-srcs?

include = test
;; cwd always included

;; broken:
exclude = test/cases
exclude = test/b
exclude = test/c
exclude = test/d
exclude = test/dune/unit_tests/fields/modules/a
exclude = test/dune/tezos/lib_requester/test
exclude = test/dune/tezos/lib_clic/test

;; non-dune dirs
exclude = bzl
exclude = convert
exclude = coswitch
exclude = deploy
exclude = docs
exclude = external
exclude = man
exclude = mibl
exclude = obsolete
exclude = private
exclude = repl
exclude = scm
exclude = src
exclude = test
exclude = tmp
exclude = tools
exclude = vendored

exclude = test/dune/stanzas/library/root_module/fail1
