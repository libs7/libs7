load("@bazel_skylib//rules:diff_test.bzl", "diff_test")

load("//:BUILD_RULES.bzl", "mustache")

exports_files(["json", "must", "resu.ref"])

diff_test(
    name  = "test2",
    file1 = "resu.ref",
    file2 = "actual.txt",
    timeout = "short"
)

mustache(
    name = "m",
    out  = "actual.txt",
    data = "data.json",
    template = "must"
)
